import { TitleSizeTwoDirective } from './title-size-two.directive';

describe('TitleSizeTwoDirective', () => {
  it('should create an instance', () => {
    const directive = new TitleSizeTwoDirective();
    expect(directive).toBeTruthy();
  });
});
